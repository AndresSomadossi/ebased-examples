{
    "exchange": {
        "exchangeCreatedTopic": "${self:custom.stackPrefix}-exchange-exchangeCreatedTopic",
        "createExchange": "${self:custom.stackPrefix}-exchange-createExchange",
        "getRate": "${self:custom.stackPrefix}-exchange-getRate",
        "reportExchange": "${self:custom.stackPrefix}-exchange-reportExchange"
    },
    "account": {
        "createDeposit": "${self:custom.stackPrefix}-account-createDeposit",
        "createDepositQueue": "${self:custom.stackPrefix}-account-createDepositQueue",
        "createWithdrawal": "${self:custom.stackPrefix}-account-createWithdrawal",
        "createWithdrawalQueue": "${self:custom.stackPrefix}-account-createWithdrawalQueue"
    },
    "wallet": {
        "getTransactions": "${self:custom.stackPrefix}-wallet-getTransactions",
        "getTransaction": "${self:custom.stackPrefix}-wallet-getTransaction",
        "getBalance": "${self:custom.stackPrefix}-wallet-getBalance"
    }
}